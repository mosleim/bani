import 'package:flutter/material.dart';

class ScanPage extends StatelessWidget {
  static const routeName = "ScanPage";
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("ScanPage"),),
    );
  }
}